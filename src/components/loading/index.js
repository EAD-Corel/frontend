import React from "react";
import { Body, Progress } from "./styles";

const LoadingComponent = () => {
  return (
    <Body>
      <Progress color="primary" />
    </Body>
  );
};

export default LoadingComponent;
